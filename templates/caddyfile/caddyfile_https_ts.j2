{% if domain and upstream %}
{{ domain }} {
    log
    tls /data/tailscale/public.crt /data/tailscale/private.key
    reverse_proxy https://{{ upstream }} {
        transport http {
            tls_insecure_skip_verify
        }
    }
}
{% else %}
# Error: domain and upstream variables are required
{% endif %}